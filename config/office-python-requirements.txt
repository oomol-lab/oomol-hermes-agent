# Office document support dependencies installed into /opt/hermes/.venv.
# Keep this file pinned so image builds are reproducible.
markitdown[docx,pptx,xlsx]==0.1.6
python-docx==1.2.0
openpyxl==3.1.5
python-pptx==1.0.2
defusedxml==0.7.1
